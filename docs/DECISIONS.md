# DECISIONS — บันทึกการตัดสินใจของโปรเจกต์ (ADR)

> **ADR = Architecture Decision Record** — บันทึกว่า "ตัดสินใจอะไร ตอนไหน เพราะอะไร"
> วิธีนี้เป็นมาตรฐานที่ทีมซอฟต์แวร์จริงใช้กันมาตั้งแต่ปี 2011 (Michael Nygard)
> เพราะโค้ดบอกได้แค่ว่า *ทำอะไร* แต่บอกไม่ได้ว่า *ทำไมไม่ทำอีกแบบ*

## ทำไมโปรเจกต์นักศึกษาต้องมีไฟล์นี้

ทีมมี 3 คน ทำงานคนละส่วน แล้วต่างคนต่างเจอสถานการณ์แบบนี้:

> "ทำไม `cfg_scale` เป็น 8 ล่ะ ค่า default ของ Forge คือ 7 นี่ ผมเปลี่ยนกลับดีกว่า"

ถ้าไม่มีที่บันทึกไว้ คนที่สามจะแก้กลับโดยสุจริตใจ แล้วอีกสองสัปดาห์ค่อยมาเถียงกันใหม่
วนแบบนี้เรื่อยๆ ไฟล์นี้ตัดวงจรนั้น — **ก่อนแก้อะไรที่มี ADR อยู่ ให้อ่าน ADR ก่อน**

ถ้าคิดว่าการตัดสินใจเดิมผิด ไม่ต้องลบของเดิม ให้เพิ่ม ADR ใหม่ที่เขียนว่า
"แทนที่ ADR-00X" แล้วเปลี่ยนสถานะของอันเก่าเป็น 🔁 ถูกแทนที่
ประวัติการเปลี่ยนใจมีค่าพอๆ กับตัวการตัดสินใจเอง

## สถานะ

| สัญลักษณ์ | ความหมาย |
|---|---|
| ✅ | มีผลบังคับใช้อยู่ |
| 🔁 | ถูกแทนที่ด้วย ADR อื่นแล้ว |
| ⬜ | เสนอไว้ ยังไม่ตกลง |

## สารบัญ

| # | เรื่อง | สถานะ | บังคับใช้อัตโนมัติโดย |
|---|---|---|---|
| [ADR-001](#adr-001--package-ที่ใช้ร่วมกันต้องล็อกเวอร์ชันตรงกันทุก-service) | เวอร์ชัน package ที่ใช้ร่วมกันต้องตรงกัน | ✅ | `tools/check_version_alignment.py` |
| [ADR-002](#adr-002--ไม่ใส่-torch--diffusers-ใน-requirements-ของ-ai-engine) | ไม่ใส่ torch / diffusers ใน requirements | ✅ | ตรวจด้วยสายตาตอนรีวิว PR |
| [ADR-003](#adr-003--cfg_scale-เริ่มต้น--8-และต้องรับ-sampler_name-กับ-seed) | `cfg_scale` = 8 และต้องรับ `sampler_name` / `seed` | ✅ | `tools/mock_forge_server.py` |
| [ADR-004](#adr-004--ไฟล์-requirementstxt-ต้องเป็น-ascii-ล้วน) | `requirements*.txt` ต้องเป็น ASCII ล้วน | ✅ | `tools/check_requirements_ascii.py` |
| [ADR-005](#adr-005--ทดสอบ-backend-ด้วย-forge-ปลอม-ไม่ผูกกับเครื่อง-gpu) | ทดสอบ backend ด้วย Forge ปลอม | ✅ | `tools/mock_forge_server.py` |
| [ADR-006](#adr-006--รายชื่อส่วนตัวเก็บในไฟล์ที่ไม่ขึ้น-git) | รายชื่อส่วนตัวเก็บในไฟล์ที่ไม่ขึ้น git | ✅ | `tools/check_no_secrets.py` |
| [ADR-007](#adr-007--pin-ด้วย--เท่านั้น-และ-requirements-devtxt-ต้องใช้--r) | pin ด้วย `==` เท่านั้น · dev ใช้ `-r` | ✅ | `tools/check_version_alignment.py` |
| [ADR-008](#adr-008--orm-ทำหน้าที่-schema-กับ-migration-ส่วน-query-เขียนเป็น-sql-ดิบ) | ORM ทำ schema/migration · query เป็น SQL ดิบ | ✅ | checklist ตอนรีวิว PR |

---

## ADR-001 — package ที่ใช้ร่วมกันต้องล็อกเวอร์ชันตรงกันทุก service

**วันที่**: 17 ส.ค. 2026 · **สถานะ**: ✅

### บริบท

โปรเจกต์แยก `requirements.txt` เป็น 3 ไฟล์ตาม service เพราะตอน deploy จริง
แยกกัน 3 เครื่อง เครื่อง backend ไม่ควรต้องลง OpenCV และเครื่อง AI ไม่ควรต้องลง Flask-Login

แต่มี package ที่ **อยู่ในหลายไฟล์** คือ `Flask` `Pillow` `requests` `pytest`

ตอนพัฒนา ไม่มีใครมี 3 เครื่อง ทุกคนลงรวมใน venv เดียวผ่าน `requirements-dev.txt`

### ปัญหาที่เกิดถ้าไม่ทำ

`pip` ไม่เตือนเวลาเจอ package เดียวกันคนละเวอร์ชัน มันติดตั้งตัวที่เจอทีหลังแล้วเงียบ
ผลคือเวอร์ชันที่แต่ละคนได้ **ขึ้นกับลำดับบรรทัดในไฟล์ และลำดับที่ลง**

อาการที่ตามมาคือบั๊กที่หาสาเหตุยากที่สุดประเภทหนึ่ง — "เครื่องผมรันได้ เครื่องคุณไม่ได้"
โดยที่ทั้งคู่ `git pull` มาจาก commit เดียวกัน

v1 ของโปรเจกต์นี้เป็นแบบนั้นจริง

### การตัดสินใจ

package ที่ปรากฏในมากกว่าหนึ่งไฟล์ **ต้องล็อกเป็นเวอร์ชันเดียวกันเป๊ะ**

ตอนนี้คือ:

| package | เวอร์ชัน | อยู่ใน |
|---|---|---|
| Flask | 3.0.3 | backend · ai-engine |
| Pillow | 12.3.0 | backend · ai-engine |
| requests | 2.34.2 | backend · ai-engine |
| pytest | 9.1.1 | backend · ai-engine · database |

### ผลที่ตามมา

- ✅ ทุกคนได้ environment เหมือนกันไม่ว่าจะลงแบบไหน
- ✅ ตรวจอัตโนมัติได้ ไม่ต้องอาศัยความจำคน
- ⚠️ เวลาจะอัปเกรด package ที่ใช้ร่วม ต้องอัปเกรดพร้อมกันทุกไฟล์ + `environment.yml`
  → ทำให้การอัปเกรดช้าลง แต่แลกกับความแน่นอน ซึ่งคุ้มกว่าสำหรับทีมขนาดนี้

### บังคับใช้อย่างไร

```bash
python tools/check_version_alignment.py
```

อยู่ใน `tools/check_all.py` และใน pre-commit hook — commit ไม่ผ่านถ้าเวอร์ชันไม่ตรง

---

## ADR-002 — ไม่ใส่ torch / diffusers ใน requirements ของ ai-engine

**วันที่**: 17 ส.ค. 2026 · **สถานะ**: ✅

### บริบท

`services/ai-engine/` ทำสองอย่างที่ต่างกันมาก:

1. **image processing pipeline 5 ส่วน** ที่เราเขียนเอง (OpenCV / NumPy / scikit-image)
   — นี่คือส่วนที่อาจารย์ให้คะแนน 40% (Lecture 1 หน้า 6)
2. **คุยกับ Stable Diffusion Forge** เพื่อสร้างภาพ

ข้อ 2 ทำให้หลายคนเข้าใจว่า "งั้นก็ต้องลง torch สิ"

### การตัดสินใจ

**ไม่ใส่** `torch` `torchvision` `diffusers` `transformers` `xformers` ใน requirements

### เหตุผล

Forge เป็น **โปรแกรมแยกต่างหาก** ที่ติดตั้งเอง มี virtual environment ของตัวเอง
และรัน web server ของตัวเองที่พอร์ต 7860 เราคุยกับมันผ่าน **HTTP** เท่านั้น
(ดู `docs/API_CONTRACT.md` หัวข้อ Backend → AI Engine)

โค้ดของเราไม่เคย `import torch` แม้แต่บรรทัดเดียว

ถ้าใส่ลงไปจะได้ผลเสียล้วนๆ:

| ผลเสีย | รายละเอียด |
|---|---|
| ขนาด | torch + CUDA ประมาณ 2.5–3 GB ต่อคน |
| เวลา | `pip install` นานขึ้นหลายสิบนาที |
| ความเปราะ | ต้องเลือก build ให้ตรงเวอร์ชัน CUDA ของการ์ดจอแต่ละเครื่อง |
| ความเข้าใจผิด | ทำให้คิดว่าต้องมี GPU ถึงจะพัฒนาได้ ทั้งที่ไม่ต้อง |

**สองคนในทีมไม่จำเป็นต้องมีการ์ดจอเลย** ซึ่งเป็นเรื่องสำคัญมากสำหรับโปรเจกต์นักศึกษา

### ผลที่ตามมา

- ✅ `pip install -r` ของ ai-engine ใช้เวลาเป็นนาที ไม่ใช่ชั่วโมง
- ✅ ทุกคนพัฒนา pipeline 5 ส่วนได้บนโน้ตบุ๊กธรรมดา
- ⚠️ การติดตั้ง Forge เป็นขั้นตอนแยก ต้องเขียนไว้ใน `INSTALL.md` ให้ชัด
- ⚠️ ถ้าวันหนึ่งอยากเรียก Stable Diffusion จากโค้ดตรงๆ (ไม่ผ่าน HTTP)
  ต้องเขียน ADR ใหม่มาแทนอันนี้ ไม่ใช่แอบเพิ่มบรรทัดใน requirements

---

## ADR-003 — cfg_scale เริ่มต้น = 8 และต้องรับ sampler_name กับ seed

**วันที่**: 17 ส.ค. 2026 · **สถานะ**: ✅

### บริบท

v1 ส่งไป Forge แค่ `prompt` `steps` `cfg_scale` `width` `height`
โดยตั้ง `cfg_scale` เริ่มต้นไว้ที่ **7** ซึ่งเป็นค่า default ของ AUTOMATIC1111

### ปัญหา

**อาจารย์แนะนำช่วง 8–14** ไว้ใน Lecture 2 หน้า 10 → 7 อยู่นอกช่วงที่สอน

และ v1 **ไม่รับ `sampler_name` กับ `seed` เลย** ทั้งที่ทั้งสองตัวเป็นหัวใจของบทเรียน:

- **sampler** — Lecture 2 หน้า 8–10 ทั้งหน้าอธิบายว่า sampler ต่างกันให้ผลต่างกัน
  ที่จำนวน step เท่ากัน (อาจารย์ยังให้ลิงก์ stable-diffusion-art.com/samplers มาอ่านเพิ่ม)
- **seed** — Lecture 2 หน้า 5–6 seed คือสิ่งที่ทำให้ **ทำผลลัพธ์เดิมซ้ำได้**
  ซึ่งจำเป็นต่อการทำตารางเปรียบเทียบในรายงาน ถ้าเปลี่ยน sampler แล้ว seed ไม่คงที่
  ก็เทียบไม่ได้ว่าผลที่ต่างมาจาก sampler จริงหรือมาจากความบังเอิญ

### การตัดสินใจ

| ฟิลด์ | v1 | v2 |
|---|---|---|
| `cfg_scale` default | 7 | **8** |
| `sampler_name` | ไม่รับ | **รับ** default `"DPM++ 2M Karras"` |
| `seed` | ไม่รับ | **รับ** default `-1` (สุ่ม) |
| `seed_used` ใน response | ไม่มี | **ต้องมีเสมอ** |

`seed_used` สำคัญเป็นพิเศษ — ถ้าผู้ใช้ส่ง `seed: -1` แล้วชอบผลลัพธ์
ต้องรู้ว่าได้ seed อะไรถึงจะทำซ้ำได้ ถ้าไม่คืนค่ากลับมา ภาพนั้นจะสร้างซ้ำไม่ได้ตลอดไป

### บังคับใช้อย่างไร

`tools/mock_forge_server.py` ตอบ 400 ถ้า `sampler_name` ไม่อยู่ในรายชื่อจริง
หรือ `cfg_scale` อยู่นอกช่วง 1–30 และคืน `seed_used` ทุกครั้ง
→ ถ้าคนที่ 1 เขียน backend โดยลืมส่ง sampler จะรู้ตั้งแต่ตอน dev ไม่ใช่ตอนสาธิต

ค่าเริ่มต้นเขียนไว้ที่ `services/backend/instance/config.py.example`

---

## ADR-004 — ไฟล์ requirements*.txt ต้องเป็น ASCII ล้วน

**วันที่**: 17 ส.ค. 2026 · **สถานะ**: ✅

### บริบท

เอกสารทั้งโปรเจกต์เป็นภาษาไทย จึงเป็นธรรมชาติที่จะเขียนคอมเมนต์ไทยใน requirements ด้วย

### ปัญหา

`pip` อ่านไฟล์ requirements ด้วย **codec ของ locale เครื่อง ไม่ใช่ UTF-8**
บน Windows ภาษาไทย locale คือ `cp874` เจอไฟล์ UTF-8 ที่มีตัวอักษรไทยแล้วพังทันที:

```text
UnicodeDecodeError: 'charmap' codec can't decode byte 0x81 in position 887
```

เป็นปัญหาจริงของ v1 (ดู `../archive/ARCHITECTURE_v1.md` ปัญหา B) และร้ายตรงที่
**คนที่เจอคือคนใหม่ที่เพิ่งเข้าโปรเจกต์** ซึ่งยังไม่รู้จักระบบพอจะเดาสาเหตุออก

### ทางเลือกที่พิจารณา

| ทางเลือก | ทำไมไม่เลือก |
|---|---|
| บอกทุกคนให้ตั้ง `PYTHONUTF8=1` | ต้องจำ ต้องตั้งทุกเครื่อง ลืมเมื่อไหร่ก็พังเมื่อนั้น |
| ให้ `pip` อ่าน UTF-8 ผ่าน flag | pip ไม่มี flag นี้ |
| **เขียน requirements เป็น ASCII ล้วน** | ✅ แก้ที่ต้นเหตุ ไม่ต้องตั้งค่าอะไรบนเครื่องใคร |

### การตัดสินใจ

คอมเมนต์ใน `requirements*.txt` เป็น **ภาษาอังกฤษล้วน** คำอธิบายไทยไปอยู่ใน `../INSTALL.md`

ยืนยันแล้วว่า `pip install -r requirements-dev.txt` สำเร็จบนเครื่อง locale ไทย
**โดยไม่ต้องตั้ง `PYTHONUTF8`**

หมายเหตุ: ข้อนี้ใช้เฉพาะ `requirements*.txt` ไฟล์ `.py` และ `.md` เขียนไทยได้ตามปกติ
เพราะ Python 3 อ่านซอร์สเป็น UTF-8 เสมอตาม PEP 3120

---

## ADR-005 — ทดสอบ backend ด้วย Forge ปลอม ไม่ผูกกับเครื่อง GPU

**วันที่**: 18 ส.ค. 2026 · **สถานะ**: ✅

### บริบท

สถาปัตยกรรมวางไว้ 3 เครื่อง Forge อยู่บนเครื่อง `192.168.1.30` เครื่องเดียวที่มีการ์ดจอ

### ปัญหา

ถ้าการทดสอบ `POST /api/generate` ต้องยิงไป Forge จริงเสมอ จะเกิดคอขวดทันที:

- คนที่ 1 ทดสอบไม่ได้ถ้าเครื่อง `.30` ไม่ได้เปิด หรือคนที่ 3 กำลังใช้อยู่
- test หนึ่งรอบใช้เวลาหลายสิบวินาที เพราะต้อง generate ภาพจริง
- **ทดสอบทางที่พังไม่ได้เลย** — จะสั่งให้ Forge จริง "ตอบช้า 30 วินาที" หรือ
  "ตอบ JSON ผิดรูป" ได้ยังไง? แต่ backend ต้องรับมือกรณีพวกนี้ให้ถูก
  (`docs/API_CONTRACT.md` กำหนดไว้ว่าต้องตอบ 502 กับ 504)

### การตัดสินใจ

ทำ `tools/mock_forge_server.py` ที่พูดภาษาเดียวกับ Forge จริง แต่:

- ใช้ **stdlib ล้วน** ไม่ต้องลงอะไรเพิ่ม ไม่ต้องมี GPU
- ตอบภาพ placeholder ที่ **seed เดียวกันได้ภาพเดียวกัน** (ทำซ้ำได้)
- **ตรวจ input เข้มกว่า Forge จริง** — ส่ง `steps: true` มาจะโดน 400
  (ดักบั๊ก `isinstance(True, int)` ที่เป็น `True` ใน Python)
- มีสวิตช์จำลองความพัง: `--delay` `--fail-rate` `--broken` `--offline`

### ผลที่ตามมา

- ✅ คนที่ 1 กับคนที่ 2 พัฒนาไปได้โดยไม่ต้องรอเครื่อง `.30`
- ✅ ทดสอบเส้นทาง error ได้จริง ซึ่งเป็นเส้นทางที่มักพังตอนสาธิตหน้าห้อง
- ⚠️ ผ่าน mock ไม่ได้แปลว่าผ่าน Forge จริง — **ก่อนส่งงานต้องทดสอบกับของจริงอย่างน้อย 1 รอบ**
- ⚠️ ถ้า `docs/API_CONTRACT.md` เปลี่ยน ต้องแก้ mock ตามด้วย ไม่งั้น mock จะโกหก

---

## ADR-006 — รายชื่อส่วนตัวเก็บในไฟล์ที่ไม่ขึ้น git

**วันที่**: 18 ส.ค. 2026 · **สถานะ**: ✅

### บริบท

repo นี้เป็น **public** ทุกคนบนอินเทอร์เน็ตอ่านได้ ทีมอยากมีตัวตรวจอัตโนมัติว่า
ชื่อจริง ชื่อเล่น อีเมล หรือรหัสนักศึกษา ไม่หลุดขึ้นไป

### ปัญหาที่ย้อนแย้ง

วิธีที่ตรงไปตรงมาที่สุดคือเขียนรายชื่อที่ต้องห้ามลงในสคริปต์ตรวจ

แต่สคริปต์ตรวจก็อยู่บน repo public เหมือนกัน → **เท่ากับเผยแพร่ข้อมูลที่พยายามปกป้อง**
และแย่กว่าเดิม เพราะตอนนี้มันอยู่ในไฟล์ที่ชื่อบอกชัดว่า "นี่คือข้อมูลอ่อนไหว"

### การตัดสินใจ

แยกเป็นสองชั้น:

**ชั้นที่ 1 — รูปแบบทั่วไป (อยู่ในสคริปต์ ขึ้น git ได้)**
ตรวจสิ่งที่เป็นข้อมูลส่วนตัว *โดยรูปแบบ* ไม่ต้องรู้ว่าเป็นของใคร:

- `C:\Users\<ชื่อ>\...` และ `/home/<ชื่อ>/...`
- path ที่มีโฟลเดอร์ผู้ใช้ของ OneDrive <!-- no-secret-check -->
- อีเมล (ยกเว้น `@example.com` และโดเมนสำหรับตัวอย่าง)
- เลข 10–13 หลักที่ยืนเดี่ยว (รหัสนักศึกษา / เลขบัตรประชาชน)
- เบอร์โทรศัพท์

**ชั้นที่ 2 — คำเฉพาะบุคคล (อยู่ในเครื่องแต่ละคน ไม่ขึ้น git)**
`tools/personal_terms.local.txt` อยู่ใน `.gitignore` แล้ว แต่ละคนใส่ชื่อเล่นตัวเองได้
ไม่มีใครเห็นนอกจากเจ้าตัว

### ผลที่ตามมา

- ✅ ป้องกันได้จริงโดยไม่เผยแพร่สิ่งที่ปกป้องอยู่
- ✅ สมาชิกใหม่เพิ่มคำของตัวเองได้โดยไม่ต้องแก้โค้ด
- ⚠️ ชั้นที่ 2 ไม่ sync กัน — แต่ละคนต้องสร้างไฟล์ของตัวเอง
  (ตั้งใจให้เป็นแบบนี้ ไม่ใช่ข้อบกพร่อง)
- ⚠️ ตัวตรวจไม่ใช่ของวิเศษ ยังต้องอ่าน diff ก่อน push ด้วยตาตัวเองอยู่ดี

### หลักการที่สำคัญที่สุดของข้อนี้

> ถ้า secret ถูก push ขึ้น GitHub public ไปแล้ว ถือว่า **หลุดถาวร**
> การลบ commit ทีหลังไม่ช่วย เพราะอาจถูก clone / cache / index ไปแล้ว
> ทางแก้เดียวที่ได้ผลคือ **เปลี่ยนค่านั้นทิ้ง** (revoke แล้วออกใหม่)

---

## ADR-007 — pin ด้วย `==` เท่านั้น และ requirements-dev.txt ต้องใช้ `-r`

**วันที่**: 17 ส.ค. 2026 · **สถานะ**: ✅

### บริบท

ตอนเขียน requirements มีสองแบบให้เลือก:

```text
Flask>=3.0        # เอาตัวใหม่กว่าได้
Flask==3.0.3      # เอาตัวนี้เท่านั้น
```

### การตัดสินใจ

ใช้ `==` ทุกบรรทัด และ `requirements-dev.txt` ต้องอ้างไฟล์อื่นด้วย `-r`
ห้าม copy รายชื่อมาวาง

### เหตุผล

**เรื่อง `>=`** — โปรเจกต์นี้ส่งงานแล้วจบ ไม่ได้ดูแลต่อ ประโยชน์ของการรับตัวใหม่อัตโนมัติ
จึงเป็นศูนย์ แต่ความเสี่ยงยังเต็ม: อีกสามเดือนคนที่ `pip install` ใหม่จะได้เวอร์ชัน
ที่ไม่มีใครเคยทดสอบ และถ้ามันพังตอนอาจารย์ตรวจ ก็จะพังโดยที่ไม่มีอะไรใน git เปลี่ยนเลย

**เรื่อง `-r`** — ถ้า copy รายชื่อมาวางใน `requirements-dev.txt` แปลว่ามีความจริง
อยู่สองที่ วันที่อัปเกรด `Pillow` ในไฟล์ service แล้วลืมแก้ไฟล์ dev
คนจะเจอเวอร์ชันไม่ตรงกันโดยไม่รู้ตัว → ผิด ADR-001 ทันที
`-r` ทำให้มีความจริงแหล่งเดียว

### บังคับใช้อย่างไร

`tools/check_version_alignment.py` ฟ้องทั้งสองกรณี:
บรรทัดที่ไม่ใช่ `==` และการที่ `requirements-dev.txt` pin เวอร์ชันเอง

---

## ADR-008 — ORM ทำหน้าที่ schema กับ migration ส่วน query เขียนเป็น SQL ดิบ

**วันที่**: 18 ส.ค. 2026 · **สถานะ**: ✅

### บริบท

ฝั่งฐานข้อมูลมี "วิธีที่ถูก" อยู่สองสายที่มาจากคนละที่ และทั้งคู่มีน้ำหนัก

**สายที่ 1 — สไลด์อาจารย์** `Lecture 7 หน้า 99–108 · FLASK + SQLite CRUD`
สอน Create / Read / Update / Delete ด้วย `fetchone()` / `fetchall()`
คือเขียน SQL เองแล้วเรียกผ่าน `sqlite3` ตรงๆ

อาจารย์ยังแจก **cheat sheet SQL 5 ใบ** (`1_SQL Basics` … `5_SQL Window Functions`)
และเกมฝึก SQL อีก 2 เว็บ (Lecture 7 หน้า 109–110)
→ ตีความได้ว่าอาจารย์**ตั้งใจให้เห็น SQL** ไม่ใช่แค่ให้ข้อมูลถูกเก็บลงตาราง

**สายที่ 2 — โค้ดเดิมของเรา** v1 นิยามตารางผ่าน SQLAlchemy ORM
(`archive/CODE_SNAPSHOT_v1.md`) และ `services/backend/requirements.txt`
pin `Flask-SQLAlchemy==3.1.1` + `Flask-Migrate==4.0.7` ไว้แล้ว

### ปัญหาถ้าไม่ตัดสินใจ

ทั้งสองสายทำงานเดียวกันได้ คนละคนจึงจะหยิบคนละสายโดยสุจริตใจ แล้วพังคนละแบบ:

| ถ้าเผลอไปทาง ORM ล้วน | ถ้าเผลอไปทาง `sqlite3` ดิบล้วน |
|---|---|
| SQL หายจากโปรเจกต์ทั้งหมด window function / `GROUP BY … HAVING` ที่ cheat sheet ใบ 2–5 สอน จะไม่โผล่ในงานสักบรรทัด | เสีย Flask-Migrate → กลับไปพึ่ง `db.create_all()` |
| เหลือแต่ `db.session.query(...)` ตอนนำเสนอไม่มีอะไรให้ชี้ว่า "นี่คือ SQL ที่เขียนเอง" | **นี่คือบั๊กที่ v1 เจอมาแล้วจริง** — PR #12 เพิ่ม 3 คอลัมน์ แต่ `.db` เก่ายังเป็น schema เดิม `create_all()` ไม่ ALTER ตารางที่มีอยู่ → **test ล้ม 3 ข้อ** |

และเป็นปัญหาข้ามเจ้าของโฟลเดอร์ด้วย — `services/backend/app/models/`
มีเจ้าของสองคนใน `.github/CODEOWNERS` (คนที่ 2 ออกแบบตาราง คนที่ 1 เรียกใช้)
ถ้าไม่ตกลงกันไว้ ก็จะแก้กลับไปกลับมา

### ทางเลือกที่พิจารณา

| ทางเลือก | ข้อดี | ทำไมไม่เลือก |
|---|---|---|
| **ก. ORM ล้วน** | เขียนเร็ว ไม่ต้องคิดเรื่อง SQL injection | SQL ที่อาจารย์แจก cheat sheet มาให้ 5 ใบ จะไม่ปรากฏในงานเลย |
| **ข. `sqlite3` ดิบล้วน ตามสไลด์เป๊ะ** | ตรงกับที่สอนที่สุด | เสีย Alembic → บั๊ก v1 กลับมา และย้ายไป PostgreSQL (แบบ 4 เครื่อง) ยากขึ้นมาก |
| **ค. แบ่งตามหน้าที่** ✅ | ได้ทั้ง migration และ SQL ที่มองเห็น | ต้องรู้ว่าเส้นแบ่งอยู่ตรงไหน — จึงต้องมี ADR นี้ |

### การตัดสินใจ

แบ่งตาม**หน้าที่** ไม่ใช่ตามความชอบ:

| งาน | เครื่องมือ | อยู่ที่ไหน |
|---|---|---|
| นิยามตาราง · ความสัมพันธ์ · การเปลี่ยน schema | ORM + Flask-Migrate | `services/backend/app/models/` · `services/database/migrations/` |
| CRUD ธรรมดาระหว่างจัดการ session/auth | ORM ก็ได้ | `services/backend/` |
| **query ที่มีตรรกะ** — JOIN, GROUP BY, HAVING, window function, รายงาน | **SQL ดิบ** | `services/database/queries/*.sql` |

**เส้นแบ่งจำง่ายๆ**: ถ้า query นั้นน่าเอาไปโชว์ตอนนำเสนอ ให้เขียนเป็น SQL

> ข้อสำคัญ: **ไม่ต้องเลือกระหว่าง SQLAlchemy กับสไลด์** SQLAlchemy เขียน SQL ดิบได้
> ผ่าน `text()` และชื่อ method เหมือนสไลด์ทุกตัว ทดสอบจริงบน Python 3.12 +
> SQLAlchemy 2.0.52 แล้ว ผ่านทั้ง `COLLATE NOCASE`, `ON DELETE CASCADE`,
> `ROW_NUMBER() OVER (PARTITION BY …)` และการกัน SQL injection

```python
from sqlalchemy import text

row = db.session.execute(
    text("SELECT * FROM users WHERE username = :n"),
    {"n": username},
).fetchone()                      # <- ชื่อเดียวกับ Lecture 7 หน้า 99-108
```

### กฎบังคับ 2 ข้อที่มาพร้อมกับการอนุญาตให้เขียน SQL ดิบ

1. **ต้องใช้ named parameter (`:name`) เสมอ ห้ามต่อสตริง**
   `text(f"... WHERE name = '{name}'")` คือช่องโหว่ SQL injection เต็มรูปแบบ
   ทดสอบแล้วว่าแบบ `:name` กันได้จริง — ยิง `x'; DROP TABLE users; --` เข้าไป
   ตารางยังอยู่ครบ
2. **ทุกการเปลี่ยน schema ต้องผ่าน migration** ห้ามแก้ตารางด้วยมือหรือ
   ด้วย `CREATE TABLE` ใน query ปกติ ไม่งั้นเครื่องแต่ละคนจะ schema ไม่ตรงกัน

### ผลที่ตามมา

- ✅ SQL ที่อาจารย์สอนปรากฏในโปรเจกต์จริง ชี้ให้ดูได้ตอนนำเสนอ
- ✅ ยังมี Alembic → บั๊ก `db.create_all()` ของ v1 ไม่กลับมา
- ✅ ย้ายไป PostgreSQL (แบบ 4 เครื่อง Lecture 4 หน้า 56) ได้ เพราะ ORM คุมเรื่อง dialect
      ส่วนไฟล์ `.sql` เขียนเป็น standard SQL เท่าที่ทำได้อยู่แล้ว
- ⚠️ **ต้องรู้ SQL สองสำเนียง** — คนอ่านโค้ดจะเจอทั้ง `db.session.query()` และ `.sql`
      แก้ด้วยกฎเส้นแบ่งข้างบน และทุกไฟล์ใน `queries/` ต้องมีคอมเมนต์บอกว่าใครเรียก
- ⚠️ **SQL ดิบไม่มีใครตรวจ type ให้** พิมพ์ชื่อคอลัมน์ผิดจะรู้ตอน runtime
      แก้ด้วยการเขียน test ให้ทุกไฟล์ใน `queries/`
- ⚠️ `.sql` ไม่ถูกจับตาโดย Alembic — ถ้าเปลี่ยนชื่อคอลัมน์ ต้องไล่แก้ไฟล์ `.sql` เอง

### บังคับใช้อย่างไร

ยังไม่มีสคริปต์ตรวจ ใช้ checklist ตอนรีวิว PR:

- [ ] มี `text("...")` หรือ f-string ที่ประกอบ SQL อยู่ใน `app/models/` ไหม → ไม่ควรมี
- [ ] ไฟล์ใน `queries/` ทุกไฟล์ใช้ `:name` ไม่ใช่การต่อสตริง
- [ ] เปลี่ยน schema แล้วมีไฟล์ migration มาด้วยไหม
- [ ] query ที่ใช้ JOIN / GROUP BY / window function ไปโผล่เป็น ORM แทนที่จะเป็น `.sql` ไหม

---

## วิธีเพิ่ม ADR ใหม่

เพิ่มเมื่อการตัดสินใจนั้น **มีทางเลือกอื่นที่สมเหตุสมผล** และ
**คนอื่นอาจแก้กลับโดยไม่รู้เหตุผล**

ไม่ต้องเขียน ADR ให้เรื่องที่มีคำตอบเดียวอยู่แล้ว เช่น "ใช้ UTF-8"

โครงที่ใช้:

```markdown
## ADR-00X — <ตัดสินใจว่าอะไร เขียนเป็นประโยคบอกเล่า>

**วันที่**: · **สถานะ**: ✅

### บริบท
สถานการณ์ตอนนั้นเป็นอย่างไร มีข้อจำกัดอะไร

### ปัญหา
ถ้าไม่ทำอะไรเลยจะเกิดอะไรขึ้น

### ทางเลือกที่พิจารณา
ตารางว่ามีทางไหนบ้าง ทำไมไม่เลือกทางอื่น   <- ส่วนที่มีค่าที่สุด

### การตัดสินใจ
สรุปสั้นๆ ให้ชัด

### ผลที่ตามมา
ทั้งข้อดี (✅) และราคาที่ต้องจ่าย (⚠️) — ต้องมีทั้งสองฝั่ง
ADR ที่มีแต่ข้อดีแปลว่ายังคิดไม่รอบ

### บังคับใช้อย่างไร
สคริปต์ตัวไหนตรวจ หรือถ้าตรวจอัตโนมัติไม่ได้ ให้เขียนว่ารีวิว PR ต้องดูอะไร
```

---

## อ่านต่อ

- [`TEAM_AND_WORKFLOW.md`](TEAM_AND_WORKFLOW.md) — ใครทำอะไร ทำงานร่วมกันอย่างไร
- [`INDUSTRY_PRACTICES.md`](INDUSTRY_PRACTICES.md) — ทีมจริงในอุตสาหกรรมเขาทำกันแบบไหน
- [`API_CONTRACT.md`](API_CONTRACT.md) — สัญญาระหว่าง service (ADR-003 มีผลกับไฟล์นี้)
- [`../tools/README.md`](../tools/README.md) — เครื่องมือที่บังคับใช้ ADR เหล่านี้
